namespace PozzBackend.Modules.Projects.Domain;

public enum ProjectStage
{
    Idea = 0,          // Just an idea
    MVP = 1,           // Minimum Viable Product
    EarlyRevenue = 2,  // Generating initial revenue
    Scaling = 3        // Scaling phase
}
