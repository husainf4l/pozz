services:
  backend:
    image: ghcr.io/husainf4l/pozz-backend:0.1.12
    ports:
      - "3400:8080"
    environment:
      - ASPNETCORE_ENVIRONMENT=Production
      - ConnectionStrings__DefaultConnection=Host=31.97.217.73;Port=5432;Username=husain;Password=tt55oo77;Database=pozz;SSL Mode=Prefer;Trust Server Certificate=true
      - JwtSettings__Secret=PozzBackend_Dev_JWT_Secret_Key_AtLeast32Chars_2026!
      - JwtSettings__Issuer=PozzBackend
      - JwtSettings__Audience=PozzApp
      - JwtSettings__ExpiryMinutes=60
      - JwtSettings__RefreshTokenExpiryDays=7
    restart: unless-stopped

  pozz-app:
    image: ghcr.io/husainf4l/pozz-app:0.1.12
    ports:
      - "3401:80"
    restart: unless-stopped

  pozz-public:
    image: ghcr.io/husainf4l/pozz-public:0.1.12
    ports:
      - "3402:3000"
    environment:
      - NODE_ENV=production
    restart: unless-stopped

  poss-admin:
    image: ghcr.io/husainf4l/poss-admin:0.1.12
    ports:
      - "3403:80"
    restart: unless-stopped
